import { atom } from 'jotai';

export const isNotClickAtom = atom<boolean>(false);
export const isSeoriDraggingAtom = atom<boolean>(false);
export const useNativeContextMenuAtom = atom<boolean>(false);
