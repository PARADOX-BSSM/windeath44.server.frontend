const protocol ='http://';
const server ='10.129.57.85';
export const auth = `${protocol}${server}:4444/auth`;//회원가압
export const user = `${protocol}${server}:4445/users`;//사용자 정보
export const memorial = `${protocol}${server}:4446/memorial`;//추모관
export const memorial_application = `${protocol}${server}:4447/memorial_application`;//추모관 신청
export const anime = `${protocol}${server}:4448/anime`;//에니메이션