const port ='http://';
const server ='10.129.57.45';
export const auth = `${port}${server}:4444/auth`;//회원가압
export const user = `${port}${server}:4445/users`;//사용자 정보
export const memorial = `${port}${server}:4446/memorial`;//추모관
export const memorial_application = `${port}${server}:4447/memorial_application`;//추모관 신청
export const anime = `${port}${server}:4448/anime`;//에니메이션